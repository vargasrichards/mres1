# Haslemere grid scan

Produces a gridded plot of the effects of switching on epi outcome

## Usage

``` r
haslemere_grid(r0_list)
```

## Arguments

- r0_list:

  list or vector of Target R0 values to scan

## Details

Essentially a wrapper for
