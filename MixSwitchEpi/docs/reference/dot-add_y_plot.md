# Add y_plot = 1/switch_rate for plotting. Inf (switch_rate==0) → finite sentinel above max finite value.

Add y_plot = 1/switch_rate for plotting. Inf (switch_rate==0) → finite
sentinel above max finite value.

## Usage

``` r
.add_y_plot(scan_df)
```
