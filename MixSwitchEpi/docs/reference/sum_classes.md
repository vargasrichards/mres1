# Find the number of hosts in each activity class through time

This is for verification mainly - there should not be change in the size
of activity classes through time in the model (ordinarily).

## Usage

``` r
sum_classes(model_output)
```
