# Compute how synchronised epidemic peaks are

Compute how synchronised epidemic peaks are

## Usage

``` r
peak_synchronicity(peak_data)
```

## Arguments

- peak_data:

  Data frame of peak times per class
