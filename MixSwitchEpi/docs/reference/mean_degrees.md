# Compute mean degree (individual)

Compute the mean number of contacts through time for a particular node.
Assumes that we have used sufficient resolution in reconstructing the
network of contacts.

## Usage

``` r
mean_degrees(reconstructed_data)
```
