# Bar comparison of outcome metrics across models

Bar comparison of outcome metrics across models

## Usage

``` r
bar_comparison(comparison_df, palette)
```

## Arguments

- palette:

  gives the colour palette to use for each model type. Should be a
  vector of colours of length equal to the number of model types (rows)
  in comparison_df. Can be generated with make_model_palette.
