# Convienience function to compute the spectral radius of a matrix

Convienience function to compute the spectral radius of a matrix

## Usage

``` r
spectral_radius(a)
```

## Arguments

- a:

  matrix

## Value

the spectral radius (max abs eigenvalue)

## See also

`compute_r0`, `compute_rt`
