# Apperoximate a switching system with a no-switch system using a single scalar epsilon parameter for assortativity/proportionate mixing.

Apperoximate a switching system with a no-switch system using a single
scalar epsilon parameter for assortativity/proportionate mixing.

## Usage

``` r
approx_epsilon(reference_parameters, initial_condition_rule, pathogen_string)
```

## Arguments

- reference_parameters:

  A list of reference parameters.
