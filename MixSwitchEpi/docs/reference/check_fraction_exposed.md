# Sanitise the exposed fraction

Makes sure that the exposed fraction is between 0 and 1.

## Usage

``` r
check_fraction_exposed(fraction_exposed)
```

## Arguments

- fraction_exposed:

  the fraction of the pop which is put in the E compartment(s)
