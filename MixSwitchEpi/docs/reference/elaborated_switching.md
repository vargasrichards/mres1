# Elaborated switching

Here we assume that w(i,j) is the flow from i -\> j. Note this is
different to some other conventions. Particularly, the ngm calculation
using Sigma matrix.

## Usage

``` r
elaborated_switching(switch_rate, num_classes, increment_scalar)
```
