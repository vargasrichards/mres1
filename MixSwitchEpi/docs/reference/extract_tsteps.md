# Extract timesteps of events (temporal network changes)

extract the timesteps (ie recorded event times) from a provided temporal
network

## Usage

``` r
extract_tsteps(temporal_net, make_uniques)
```

## Arguments

- temporal_net:

  the temporal data

- make_uniques:

  boolean, whether to condense the times of events
