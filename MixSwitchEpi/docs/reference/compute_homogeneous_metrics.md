# Computes the outcome metrics for a homogeneous population

That is, the outcome metrics for a population where each individual has
the same activity. This is equivalent to a mass action single pop.

## Usage

``` r
compute_homogeneous_metrics(base_params)
```

## Arguments

- base_params:

  the basic parameters: beta, sigma, gamma
