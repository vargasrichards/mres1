# Pivot SEIR epidemic df + prepare for plotting

Pivots the epidemic dataframe into suitable plotting format and then
binds an epidemic label

## Usage

``` r
pivot_seir(df, model_name)
```

## Arguments

- df:

  the epidemic dataframe

- model_name:

  string descriptor for the model
