% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_time.R
\name{heatmap_through_time}
\alias{heatmap_through_time}
\title{Simulate and plot a class × time infection heatmap}
\usage{
heatmap_through_time(
  parms_list,
  time_range = NULL,
  save = TRUE,
  output_file = "output/class_time_heatmap.pdf",
  width = 8,
  height = 6
)
}
\arguments{
\item{parms_list}{a named list of 1 or 2 parameter lists,
each suitable for \code{run_system_wparms()}.
Names are used as panel titles.}

\item{time_range}{optional numeric vector \code{c(t_start, t_end)}
to restrict the y-axis.  Defaults to the full simulation range.}

\item{save}{logical; if \code{TRUE} saves a PDF to \code{output_file}.}

\item{output_file}{path for the PDF.}

\item{width, height}{PDF dimensions in inches.}
}
\value{
A patchwork object (invisibly).
}
\description{
For each parameter set supplied, runs the SEIRS model and
produces a heatmap with activity class on the x-axis,
time on the y-axis, and \code{(E + I) / class_size} as the
fill (inferno palette).
}
\details{
When two parameter sets are provided the heatmaps are placed
side-by-side for direct comparison.
}
